import domReady from '@wordpress/dom-ready';
import { render } from '@wordpress/element';

function Editor() {
    return "Hello Editor from JS";
}

domReady(function () {
    render(<Editor />, document.getElementById('getdave-sbe-block-editor'));
});